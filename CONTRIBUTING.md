# Contributing

## Workflow

1. Read `docs/PROPOSAL.md` for product scope.
2. Read `docs/IMPLEMENTATION_CHECKLIST.md` for execution order.
3. Use `docs/IMPLEMENTATION_BACKLOG.md` to pick the next issue-sized unit of work.

## Development Commands

```bash
npm install
npm run build
npm run test
npm run lint
```
